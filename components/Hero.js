import Image from "next/image";import Link from "next/link";import Container from "./Container";export default function Hero({image,eyebrow,title,text,primary,secondary,full=false,alt=""}){return <section className={`relative overflow-hidden bg-[#222] text-white ${full?"min-h-[92svh]":"min-h-[70svh]"}`}><Image src={image} alt={alt} fill priority className="object-cover" sizes="100vw"/><div className="hero-gradient absolute inset-0"/><Container className={`relative z-10 flex ${full?"min-h-[92svh]":"min-h-[70svh]"} items-end pb-12 pt-28 md:pb-20`}><div className="max-w-4xl"><p className="eyebrow text-white/75">{eyebrow}</p><h1 className="display mt-4 text-[clamp(2.6rem,7vw,5.5rem)]">{title}</h1>{text&&<p className="mt-6 max-w-2xl text-base leading-7 text-white/85 md:text-lg">{text}</p>}<div className="mt-8 flex flex-wrap gap-3">{primary&&<Link href={primary.href} className="focus-ring bg-white px-5 py-3 text-sm font-semibold text-[var(--ink)]">{primary.label}</Link>}{secondary&&<Link href={secondary.href} className="focus-ring border border-white/60 px-5 py-3 text-sm font-semibold text-white backdrop-blur-sm">{secondary.label}</Link>}</div></div></Container></section>}