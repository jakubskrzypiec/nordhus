import Image from "next/image";import Link from "next/link";import {BLUR_DATA_URL} from "@/lib/image";export default function ModelCard({model}){return <article className="reveal group"><Link href={`/modele/${model.slug}`} className="focus-ring block"><div className="relative aspect-[4/3] overflow-hidden bg-white"><Image src={model.image} alt={`${model.name} — dom modułowy ${model.area} m²`} fill className="object-cover transition-transform duration-700 ease-[cubic-bezier(.22,1,.36,1)] group-hover:scale-[1.04]" sizes="(min-width: 768px) 50vw, 100vw" placeholder="blur" blurDataURL={BLUR_DATA_URL}/></div><div className="flex items-start justify-between gap-5 border-b border-[var(--line)] py-5"><div><h3 className="text-link w-fit text-xl font-normal">{model.name}</h3><p className="mt-1 text-sm leading-6 text-[var(--muted)]">{model.area} m² · {model.short}</p></div><p className="shrink-0 text-sm font-normal">{model.price}</p></div></Link></article>}